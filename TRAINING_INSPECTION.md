# 数据审计（Supervisor 工作台）· 工作记录

「数据审计」对应《Supervisor 已确定设计》里的 **Supervisor 工作台**（设计文档里把这项能力叫「巡检」，本原型与前端的用户可见文案统一叫「审计」，代码里的实体名仍是 `inspection*`）：它是一个像 Codex 一样的对话工作台，管理者问一句「最近培训情况怎么样」，Agent 先补齐条件（时间、地区、品类、关注方面），再通过白名单工具取数，执行确定性审计规则，最后给出**带数据出处的报告**。参考文档在 `/Users/lee/Projects/SalesBoost/Supervisor/`（`确定设计.md` / `待确定设计.md`）。

- 学习、练习、考试（以及接入中的媒体采集）任务统一转换为审计任务对象；
- Agent 只负责理解问题、补齐条件、选择工具与组织表达；指标、数字、权限与风险结论由规则和 ADM 返回的数据决定，模型不能编造；
- Agent 不修改培训任务、人员、组织与成绩，只写独立的审计记录；
- 当前版本仍是本地演示数据 + `localStorage`，工具返回与模型话术都是前端模拟，但结构、口径与权限语义按设计对齐。

## 交互形态：与「生成课件」统一的卡片式对话

数据审计页默认就是一个 **AI 员工的对话入口**（Codex 式的极简首屏）：
首屏只有徽章「数据审计 Agent」+ 一句话「想问什么，直接说。」+ 一个输入框 + 可以这样问的示例 chip + 三步路线（你说一句 / 我确认口径 / 取数后给你汇报），
没有页面级标题栏、没有工具条、没有平级 tab 抢首屏。

1. 提交后初始卡**只淡出**（0.7s，不位移），审计工作台**淡入 + 上浮 8px**（0.9s），左右面板错峰入场 —— 与生成课件的 `cw-swap` 完全一致。
2. 工作台顶部是四段轨道：**理解需求 → 确认口径 → 取数核对 → 汇报结论**，右侧显示「执行中 / 结论已留痕」、**审计档案**入口和「重新开始」。
3. 左栏是「审计 Agent」的对话流：用户气泡 → **澄清卡** → 口径确认 → **查询过程轨迹** → **汇报卡**；底部常驻输入框可以继续追问。
4. 右栏是「审计依据」：本次口径（范围 / 周期 / 关注方面 / 品类 / 数据版本 / 规则集 / 单日承受线）→ **已经拿到的数据**（每查完一步追加关键数字）→ 可以接着问 → 白名单工具（折叠）。
5. 双栏中间有**交换左右面板**按钮（顺序记在 localStorage），窄屏双栏叠起来时自动隐藏。
6. 视觉与生成课件共用一套令牌（`--cw-*`）：暖纸底 + 陶土色主色 `#b25d49`、24px 面板圆角、`cw-arrive` 入场动画、事件标签用等宽字体。

### 想让老板怎么看到效果（三段式）

1. **澄清**（`clarificationsFor()`）：只问必要参数，一张卡里按顺序问，答完一条自动展开下一条；每条都可以点选项、在输入框里直接打字，或点「先跳过」用默认值（时间→本周、范围→全国、品类→全部品类、关注方面→全部方面）。已答的问题收成一行「✓ 本周」。
2. **过程**：查数据不是黑箱——每一步在轨迹里有一句人话旁白（「把每个区域横向比一遍，看哪里最挤」）、工具名、一行结果摘要；点「看数据」展开这一步的结论、指标、明细表、口径说明、数据来源，以及权限码 / traceId / 扫描范围 / 耗时这类技术细节。
3. **汇报**：最后给的不是一屏原始数据，而是一张**汇报卡**：一段话结论（谁这段时间排了多少任务、压在多少人身上、人均多少、问题集中在哪、超容量的有多少人、最挤的是哪天、建议先做哪一步）→ 6 个核心数字 → **各区域人均排期**（横向条 + 容量基线，超容量的区域标红）→ **这一周每天最忙的人**（虚线是单日承受线，超过的柱子标红）→ 需要你知道的 N 件事（规则编号 + 原因 + 证据 + 出处 + 看任务详情）→ 建议怎么做 → 「汇报背后的数据」「这次怎么查的」两处折叠 → 保存审计记录 / 每周一自动审计 / 导出报告 / 标记已确认。

### 两个视图

- **Agent 对话（默认）**：整屏留给对话、查询过程与汇报。
- **审计档案**：指标卡、最近一次审计、审计工作记录、区域工时审计、自定义审计等留档视图；右上角「回到 Agent 对话」切回来。两个视图都保持挂载，来回切不会丢对话。


## 页面信息架构

页面分两个视图（不是平级 tab）：**Agent 对话**（默认）与**审计档案**。

| 视图 / 标签页 | 回答的问题 | 主要内容 |
| --- | --- | --- |
| Agent 对话（默认，无页面标题栏） | 老板问的那句话，Agent 怎么理解、怎么查、得出什么结论？ | 澄清卡 → 查询过程轨迹 → 汇报卡（一段话结论 + 数字 + 两张图 + 关键发现 + 建议 + 明细折叠）；右栏是审计依据（口径 / 已拿到的数据 / 可以接着问 / 白名单工具折叠） |
| 审计档案 › 审计工作台 | 观测指标、最近一次审计、历史记录在哪里？ | 最近一次审计摘要、四张指标卡、审计工作记录（含「查看问题任务」弹窗）、自定义审计创建 |
| 审计档案 › 区域工时审计 | 不同区域的任务给 BA 造成了多少时间负担？ | 区域对比表（人均分钟 / 超载人数 / 单日峰值 / 任务数）→ 区域明细（任务占用 / 重复内容 / 超载的人）→ 每人每日分钟下钻 |
| 审计档案 › 自定义审计（每个需求一个标签页） | 我自己关心的那类问题，这周给 BA 添了多少负担？ | 状态摘要（发现的问题 / 受影响的 BA / 人均分钟 / 最近一次运行）→ 现在关注到的问题（可跳区域工时审计定位）→ 运行记录（批次展开见逐任务结论） |

- 审计档案里的顺序固定：**最近一次审计 → 指标卡 → 审计工作记录 + 自定义审计**；右上角「回到 Agent 对话」切回默认视图，两个视图都保持挂载，来回切不会丢对话。
- 汇报卡里点「看「任务名」」会切到审计档案的区域工时审计并定位、高亮该任务。
- 任务行点「查看详情」先开只读的任务详情弹窗（任务类型、负责人、起止与频次、单次时长、下发范围与命中人数、品类、本周排期、内容资源、关联任务、执行情况、人员名单、缺字段提示），弹窗右下角「向创建者发送调整建议」会模拟 Agent 生成一封写给任务创建者的站内信草稿（问题、建议动作、影响范围），确认发送后提示「已进入消息中心 › 我的消息」（原型演示，不真的发送）；审计只给建议，不改任务数据。
- 下钻只在区域工时审计里：区域 → 任务 → 人（每日分钟构成），不做独立的员工 / 区域档案页。
- 自定义审计标签页排在「审计工作台 / 区域工时审计」之后：名称 + 未读小圆点 + × 关闭（二次确认），最多同时保留 6 条。

## 与 Supervisor 设计的对应关系

| 设计里的角色 | 原型里的体现 |
| --- | --- |
| 管理者 | 页面顶部的角色切换（总部培训师 / 区域培训师主管 / 区域经理），决定可见区域与可执行动作 |
| SalesBoost-vue 工作台 | 本页：对话、补问、执行过程、报告、审计记录都在这里展示 |
| SalesBoost-adm | 工具层里的 `owner = adm` 工具：每次调用展示权限码与「权限校验通过」，区域账号问其它区域会被直接拒绝 |
| Supervisor 服务 | 工具层里的 `owner = supervisor` 工具（执行审计规则）与模型话术（补齐条件、组织表达） |
| SSE 事件 | 每条消息 / 工具卡上带 `run_started`、`clarification_required`、`tool_call_started`、`tool_call_completed`、`inspection_started`、`finding_created`、`report_completed` 事件标签 |
| 统一返回 meta | 工具卡展示 `traceId`、扫描范围、耗时与数据来源，便于按 requestId / traceId 对账 |
| 不修改业务数据 | 报告动作只提供「保存审计记录」「每周一自动审计」「导出报告」「标记已确认」，没有任何写入任务的入口 |

## 工作台交互：补问 → 工具调用 → 报告

1. **补齐条件**（`clarificationsFor()`）：时间（本周 / 上周 / 下周）必问；总部账号没点明地区时问范围；问法含糊（「情况怎么样」「有什么问题」）时问关注方面；提到产品而没点名品类时补问品类。每一步既可以点选项，也可以直接打字（「上周」「南区」「全部品类」都能解析）。
2. **选择工具**（`planFor()`）：按关注方面挑工具链——全部方面跑 7 个工具，工时只跑范围 / 概览 / 区域对比 / 规则，数据完整度跑范围 / 字段核对 / 概览 / 规则，重复布置与品类覆盖各有自己的链路。
3. **执行与留痕**：每个工具按「调用中 → 返回」两态展示，工具卡上给出工具名、归属（ADM / Supervisor）、权限码、实参、`traceId`、扫描范围、耗时、结论、指标、可展开的明细表、口径说明与数据来源。
4. **报告**：`buildAuditReport()` 把结论组织成「审计结论 / 命中规则 / 影响范围 / 人均工时 / 需人工确认」+ 关键发现（含规则编号、原因、证据、数据出处）+ 整改建议 + 数据来源与口径。
5. **落库与定时**：报告上的「保存审计记录」会作为一次工具调用产出 `save_inspection_record` 卡片（含记录编号、写入范围、规则版本、是否修改任务）；「每周一自动审计」产出 `save_schedule` 卡片（结构化条件 + 飞书群消息草稿，注明由 ADM 发送）。

## 工具白名单

`src/lib/auditTools.ts` 登记 9 个工具，没有「执行任意 SQL」这类入口。除 `run_inspection_rules` 是本地确定性计算，其余都标注为 ADM 侧工具并带权限码。

| 工具 | 归属 | 权限码 | 作用 |
| --- | --- | --- | --- |
| `query_scope` | ADM | `supervisor:scope:read` | 查询地区、产品与组织范围，确认可查边界 |
| `check_data_gaps` | ADM | `supervisor:data-quality:read` | 检查预计时长 / 逐人分配 / 频次 / 负责人 / 版本 / 推送 / 回传是否缺失 |
| `query_training_overview` | ADM | `supervisor:training-overview:read` | 查询培训概览：任务数、覆盖 BA、工时 |
| `query_region_comparison` | ADM | `supervisor:region-comparison:read` | 查询区域对比：人均、超容量、完成率、任务数 |
| `query_product_stats` | ADM | `supervisor:product-stats:read` | 查询产品与品类统计：品类 × 区域覆盖与命中问题 |
| `query_learning_completion` | ADM | `supervisor:completion:read` | 查询学习与考试完成情况：完成率、逾期率、考试分 |
| `run_inspection_rules` | Supervisor | `rule-set:v1` | 执行 A–G 确定性审计规则 |
| `save_inspection_record` | ADM | `supervisor:record:write` | 保存独立审计记录，不改任务 |
| `save_schedule` | ADM | `supervisor:schedule:write` | 保存定时报告条件（由 ADM 定时触发、发送飞书） |

- 侧栏的「工具白名单」可以**单独调用**任意工具，用来验证口径：`query_scope` 验证可见范围、`check_data_gaps` 验证字段完整度、`cross_check` 类工具验证区域差异。
- 每个工具都会在返回值里带「口径说明」与「数据来源」，例如「工时口径 = 内容预计时长 × 周期内应完成次数 + 附加题；缺时长的人不计入」。

## 异常

异常按设计分三类，工具卡上用统一的错误码与说明展示（可重试 / 是否需要用户补充信息）：

- **权限问题**：区域账号问别的区域时，`query_scope` 直接返回 `PERMISSION_DENIED`，页面写明「当前账号是南区培训主管，只能查看雅加达南区与全国任务」，并给出可问的范围；不绕过权限取数。
- **业务数据问题**：字段缺失返回 `DATA_FIELD_MISSING`，明确「缺字段只标数据不足、不算违规」，并给出去哪里补齐。
- **系统问题**：留给接口层（超时 / 限流）使用，当前原型未触发。

## 历史周期视图

演示数据只排了当前一周，直接审计上周会得到空结果。`src/lib/auditHistory.ts` 在**只读派生**的基础上补一层历史视图：

- 审计周期全部落在当前周时直接用原状态；
- 含历史周期时，把平台创建的任务整体前移 N 周生成副本（id 加 `-hN` 后缀，任务关系一起重指向），按审计周裁剪起止时间，避免长期任务在两个周期重复计入；
- 副本按任务类型补齐完成回传与考试成绩（学习 88% / 考试 82% / 练习 72% / 媒体采集 60%，长期任务 42%），因此「上周」看起来是一个**已经回传、可复盘完成情况**的周期；
- 结论只保留「该结论所属周期里真的有排期」的任务，避免跨周期重复计数；报告里的任务链接会把 `-hN` 还原成原任务 id。
- 时间选项目前只有本周 / 上周 / 下周：设计里的定时汇报默认就是「每周一 09:00 复盘最近一个完整周」，多周期趋势对比属于后续设计项。

## 审计任务数据模型

`src/lib/inspectionTypes.ts` 定义统一契约。每个任务至少包含：

- **任务类型**：学习 / 练习 / 考试（媒体采集作为接入类型）。
- **内容资源**：课件、剧本、数字人、金句、试卷及其预计时长；时长缺失即数据不足。
- **分发范围**：全国、区域、门店、角色、人群策略；`resolvedPersonIds` 是发布时解析出的**实际人员快照**，并记录快照版本。
- **时间规则**：开始时间、结束时间、每日 / 每周频次与达标次数。
- **任务权重**：必修、选修、补训、复训。
- **负责人**：总部、区域、门店三级负责人。
- **任务关系**：同源内容、前置任务、互斥任务、重复任务与是否显式声明顺序。
- **执行结果**：逐人完成次数、考试成绩、推送与完成回传时间。

员工侧聚合字段由 `personAggregates()` 计算：当前周期任务数、预计分钟、剩余分钟、每日峰值、必修任务数、截止日重叠数、同内容重复数、完成率与逾期率。

## 审计规则

规则 ID 与阈值均进入风险输出（`metrics` / `ruleParams`），可追溯、可复核。默认阈值见 `inspectionPolicy`（容量 90 分钟、单日 60 分钟、单任务周投入 45 分钟、集中窗口 4 天内 ≥ 3 项等）。

| 分类 | 规则 |
| --- | --- |
| A 负荷合理性 | A1 单人预计分钟超过区域容量；A2 少数员工负荷明显高于区域平均（内部按前 10% 口径计算，界面用「高负荷人群人均」表达）；A3 单人单日分钟超过阈值；A4 周期频次 × 单次时长超出合理投入；A5 必修任务与考试集中在同一窗口；A6 区域任务总量明显高于其他区域 |
| B 时间合理性 | B1 剩余时间不足以完成要求次数；B2 截止日期与频次不匹配；B3 多个任务集中截止；B4 发布时间晚于有效周期起点；B5 长期有效任务没有复查或失效机制 |
| C 人群合理性 | C1 命中人数与预期人群差异过大；C2 区域任务误命中其他区域员工；C3 已完成同源任务的员工再次被安排相同内容；C5 命中不均衡（原来的 C4「角色不适用」按业务反馈下线，不再产出） |
| D 内容与任务关系 | D1 学习与考试之间没有明确顺序；D2 同一场景被多个练习任务重复要求；D3 考试发布前缺少对应学习内容或练习任务；D5 任务名称、内容与类型不一致 |
| E 执行风险 | E1 当前进度不足以在截止前达标（含逾期）；E2 完成率偏低但实际是高负荷区域导致；E3 区域完成率异常低，可能推送或人群策略未生效；E4 已发布任务缺少负责人或提醒策略；E5 任务修改后仍有员工处于旧版本 |
| F 数据不足 | F1 关键字段缺失（预计时长 / 逐人分配 / 频次 / 负责人 / 版本 / 推送 / 回传）；F2 缺少容量基线与足够历史 |
| G 品类覆盖 | G1 某个品类只在部分区域有任务：例如「彩妆」只在北区安排了任务，南区 / 巴厘岛区 / 泗水区这一期接触不到，需要确认是漏排还是有意只在部分区域上线 |

风险等级：**高风险**阻断发布或要求总部确认；**中风险**允许发布但需负责人确认；**低风险**提示优化；**数据不足**不做结论。

## Agent 输出格式

每条风险使用固定结构，界面上可直接展开证据：

> **结论**：建议调整
> **原因**：南区 12 名员工本周预计人均超过 90 分钟容量（区域均值 269 分钟）。
> **证据**：已确认容量 90 分钟 / 人 / 周（来源：区域「雅加达南区」· 容量基线）；Siti Aminah 1：314 分钟 = 新品核心卖点与附加题 35 分钟 + …（来源：员工 south-1 · 本周任务快照）。
> **建议**：将非必修任务顺延至下周，或下调每周练习频次。
> **影响**：若顺延非必修任务，预计 12 人回落至 90 分钟以内。
> **需要确认**：Fitriani（区域负责人）。

每条风险提供三个动作：调整（发布前模拟）、打开原任务编辑、标记合理例外 / 进入处置；影响范围以「影响 N 人 · 区域」文字摘要呈现，本版不再跳到员工 / 区域下钻页面。

## 发布前模拟

`simulateTaskChange()` 在内存中复制状态并应用补丁（目标人群、频次、截止时间、预计时长、权重、状态、提醒策略），重新运行规则后返回：

- 受影响人数、区域人均分钟、最高单人分钟、单日峰值的前后对比；
- 各区域命中人数、人均分钟、超容量人数的变化；
- 风险解除、风险新增、风险等级变化清单与文字结论。

模拟不会写入任务；确认处置后才递增版本、保留变更字段并重新审计。

## 处置（通过弹窗完成）

处置不再占用独立标签页，当前入口是任务详情弹窗右下角（原「调整任务」按钮已按反馈换成「向创建者发送调整建议」，`DispositionDialog` 仍保留，待确认后决定是否重新挂入口）。处置动作共 8 类——调整任务、调整人群、调整截止时间、降低频次、合并重复任务、暂停发布、标记为合理例外、转交负责人。

- 处置记录保留：操作人、原始任务版本、调整原因、字段级变更摘要、预计影响、重新审计结果（哪些规则解除、哪些仍命中、是否新增）。
- 例外：区域提交后进入待总部审批，批准后在有效期内生效，到期自动恢复审计状态。
- 权限：总部可处理全部演示任务；区域培训角色只能调整本区域任务，全国任务需要转交；区域经理只读；外部接入任务必须先补齐字段契约。
- 风险台账按 `周期:分类:规则:对象` 去重，记录首次发现、最近命中、状态流转、复发次数与历史事件。

## 审计工作记录

每次审计都会把「这一次做了什么」写进工作记录（`InspectionState.inspectionRuns`），最多保留最近 60 个批次：

- 批次字段：记录时间、最近一次审计时间、连续审计次数、触发方式（手动 / 自动）、覆盖周期、覆盖任务 ID 与数量、字段待补齐任务数、风险快照、本次新增与解除的风险、风险等级变化、逐任务结论（`taskResults`：任务 ID、最严重等级、命中的规则编号）、执行人与角色标签。
- **有变化才新增**：没有新增风险、没有解除、没有等级变化、覆盖任务集合未变时，只把最近一条的 `lastSeenAt` 与 `repeatCount` 往前推；手动点「立即审计」始终新增一条；跨天必新增一条，保证每天至少有一次工作记录。
- 同一个问题同时命中本周与下周时只记一条，避免同一条结论重复汇报。
- 读取浏览器本地快照时的初始化审计传入 `record: "none"`，刷新页面不会产生假批次。
- 演示数据预置近 7 天 4 个批次（含一条已解除的历史问题），打开页面即可看到工作记录。

## 对话 Agent

`src/lib/inspectionAgent.ts` 暴露唯一入口 `answerInspectionQuestion(state, actor, question, history?, options?)`，本轮由本地确定性规则实现：

- 意图覆盖：最近审计了哪些任务 / 现在有哪些任务需要我处理 / 风险最高的任务 / 哪些任务数据不全 / 上次审计到现在有什么变化 / 某区域情况 / 某任务为什么被标记；识别不了时返回「能回答的范围 + 预设问题」，不编造结论。
- 每条回答带 `citations`（口径说明、周期、任务名）与 `followUps`；从审计结论点任务会在区域负担里自动定位并高亮。
- 可见范围与页面一致：区域角色只能看到本区域与全国任务；问其他区域时返回权限说明，不泄露数据。
- 界面按异步设计（加载态 + 约 400ms 模拟延迟），接入真实模型时只替换该函数实现，问题、回答结构与工作记录都不需要改动。
- 创建向导：说「新建 / 创建一个审计」进入 `src/lib/inspectionRequirements.ts` 的 `matchRequirementIntent()` 判定，随后四步选项反问（关注方面 → 范围 → 周期 → 起止时间）与名称确认都走本地确定性逻辑，不依赖模型。

## 自定义审计需求（Agent 创建）

- 对话创建：`和审计Agent对话` → 预设「新建一个审计需求」，或在输入框里说「帮我新建一个审计」。向导四步：关注方面（重复布置 / 时间集中 / 任务量超了 / 包含特定品类，可多选，含「以上都要」）→ 范围（全国或当前账号可见的某个区域）→ 周期（每天 / 每周一 / 每月 1 日 08:00）→ 起止时间（长期 / 连续 4 周 / 自定义日期），确认页可以改名称（最多 12 字）。随时可「取消创建」。
- 每一步既能点选项，也能**直接打字**（输入框全程保留，提示语跟着步骤变）：`parseAspectsFromText` / `parseRegionFromText` / `parseCadenceFromText` / `parsePeriodFromText` 把这四步的自然语言解析成标准答案（例如「重复布置和时间集中都要盯」「南区」「每周一」「从今天起连续 4 周」「9 月 20 日到 12 月 31 日」「长期」），确认页再用 `parseReplyCommand` 认「确认 / 取消 / 返回修改」。解析不出来时 Agent 只说清能听懂什么并留在原步骤，不跳步、不猜。
- 计算口径：`requirementRisks()` 复用 `evaluateRisks()`，按关注方面（规则分组）+ 品类 + 区域筛选，并把人数与区域收窄到需求范围；问题清单只看未解决的结论，展示仍是业务口径（「N 人要把相同内容做两遍」这类），不出现规则编号。
- 「包含特定品类」：任务模型带 `InspectionTask.categories`（演示数据：新品 / 护肤 / 敏感肌 / 彩妆 / 服务力 / 异议处理 / 销售话术）。选了这个方面后向导多问一步品类（选项来自 `requirementCategories()` 扫出的真实品类，可多选、可打字、可「以上都要」）：与规则方面叠加时两个条件同时成立，只选品类时等于「这些品类上的问题都要」。
- 运行记录：`runRequirement()` 记录扫描任务数、命中问题数、受影响人数、人均分钟（关注任务里这些人本周的计划分钟，缺预计时长的人不计入并单独标注）与逐任务结论；与上次结论一致时只推进 `lastSeenAt` 与 `repeatCount`，出现新增 / 已解决才新增一条，最多保留 30 条。
- 调度：周期固定 08:00（Asia/Jakarta）。`requirementDue()` 判断今天是否到点且当天还没有自动跑过，打开页面时补跑一次；暂停、未开始或已过结束日期都不跑；过了结束日期自动变「已结束」。
- 存储与权限：独立 `localStorage` 键 `salesboost.training-inspection.requirements.v1`，与主审计存档解耦，最多 6 条，单条不完整自动跳过；总部看全部需求，区域账号看本区域与全国需求，且创建时范围直接锁定为本区域。

## 自动审计频次

- 策略字段 `InspectionPolicy.autoRunMinutes`（默认 30 分钟）决定自动审计间隔；总览「最近一次审计」右上角显示成「每 30 分钟自动审计一次，有变化才记新批次」（整小时显示为「每 2 小时」，并给出「想改对 Agent 说」的提示）。
- 定时器（`startInspectionScheduler()`）按这个间隔递归排期，改完下一轮生效；结论没变化时仍只推进 `lastSeenAt` 与 `repeatCount`，不刷批次。
- 对话修改：问「自动审计多久跑一次？」回答当前设置；说「把审计频率改成每 2 小时一次」会返回一条 `action: { type: "set-cadence", minutes }`，界面收到后写回策略（Agent 自己不改数据）。`formatCadence()` / `parseCadenceMinutes()` 支持「半小时」「15 分钟」「2 小时」「每小时」「每分钟」等说法，范围收敛在 1 分钟到 24 小时之间。

## 数据来源

学习 / 练习 / 考试 / 采集任务页通过 `syncInspectionSource()` 把原始任务摘要同步为审计任务。缺少逐人分配、预计时长、频次或负责人的摘要只输出「数据不足」，不会被判定为违规，也不会混入负荷计算。总览的「数据来源」入口列出同步过来的任务与待补齐字段，并可跳回原任务页面。

## 运行与持久化

- 状态保存在浏览器 `localStorage` 键 `salesboost.training-inspection.v2`；读取失败时降级为当前会话并提示。快照缺少 `inspectionRuns` 时按空工作记录兼容，演示数据版本升级会重建含工作记录的演示数据。
- 自定义审计单独存在 `salesboost.training-inspection.requirements.v1`：演示数据重置不会删掉用户自己建的审计；单条记录损坏只跳过该条，不白屏。
- 页面打开期间按 `policy.autoRunMinutes`（默认 30 分钟）自动审计，改完下一轮生效；任务、完成记录与处置动作变化立即触发重算。
- 演示数据中所有数值（容量、历史、完成回传、待复核量）均为本地 mock，不代表客户确认的管理标准。
- 日期统一按 `Asia/Jakarta` 解释，周一为周期起点；周末与节假日日历尚未接入。

## 验收对照

| 验收标准 | 实现位置 |
| --- | --- |
| 新建或修改任务后一次审计能识别类型、人群、周期、频次、时长与负责人 | `missingFields()` + 审计工作记录「查看异常任务」弹窗里的「数据不足」分节 |
| 管理者能看到 Agent 审计了哪些任务、结论与依据 | 审计总览「审计工作记录」批次展开 + 「任务覆盖清单」+ 对话 Agent |
| 结论无变化时不刷屏、出现变化时可追溯 | `recordInspectionRun()` 的合并 / 新增规则与批次内的新增、解除、等级变化明细 |
| 影响范围可核对 | 风险卡与任务覆盖清单里的「影响 N 人 · 区域」文字摘要（本版不做员工 / 区域下钻页面） |
| 所有高风险结论都有可展开的证据明细 | 风险卡「证据」列表，逐条给出数据出处 |
| 调整任务参数后风险与负担即时重算 | `simulateTaskChange()` + `applyDisposition()` 提交后的复查结论（`recheck`），单测 `inspectionEngine.test.ts` 覆盖 |
| 发现某条任务排得过重，能提醒到任务创建者 | 任务详情「向创建者发送调整建议」→ `buildAdjustmentSuggestion()` 生成站内信草稿（收件人取 `taskCreatorOf()`） |
| 数据缺失不会被误判为违规 | F 类规则与员工 `unknownTaskCount`，超容量判定自动排除缺时长员工 |
| 任务调整后保留版本、操作人、原因与复查结果 | `applyDisposition()` + 处置记录表 |
| 总部与区域角色只能处理有权限的任务和员工 | `canSeeRegion` / `canSeeTask` / `canActOnTask` / `canApproveException` |
| 首屏回答最危险任务、影响谁、下一步怎么处理 | 总览「现在最需要处理的风险」结论条 |
| 用户能自己建一条审计，并单独关注它的运行情况 | `AgentPanel` 创建向导 + `runRequirement()` + 自定义审计标签页（状态摘要 / 问题清单 / 运行记录） |

## 验证命令

```sh
npm run lint
npm run test:inspection
npm run build
```

浏览器验证覆盖两个标签页、对话 Agent 的预设与追问、批次展开、任务覆盖清单筛选、发布前模拟与处置提交、复查结果、区域与只读角色权限、桌面 1440px 与移动端 390px 布局；截图保存在 `output/playwright/`。

## 生产接入

1. 后端补齐统一任务、人员归属与在岗状态、完成明细、负责人、时长、策略和例外数据；任务编辑使用版本控制并保留历史分配快照。
2. 规则与状态机部署到受鉴权的后端，区域范围由登录身份决定，客户端角色与责任人字段不可信。
3. 接入任务、人员与完成事件，5 分钟内增量计算；服务端每日全量校验，浏览器定时器不承担后台审计。
4. 工作记录与对话记录随审计批次一起落库（按租户 / 角色做行级权限），由服务端生成批次并对外提供查询接口。
5. 可选模型只接收结构化事实与少量证据，用于解释与简报；规则结论、权限、责任归属和状态机保持确定性。接入时替换 `answerInspectionQuestion` 的实现并保持返回结构不变。


## 本轮改动文件

- `src/lib/auditTools.ts`：工具白名单、工具实现、澄清补问、审计计划、报告生成；新增 `AUDIT_STEP_NARRATION`（每步人话旁白）、`AuditRegionPoint` / `AuditDayPoint` 与汇报文案 `briefing`。
- `src/lib/auditHistory.ts`：历史周期视图与 `scheduledIn()`。
- `src/pages/training-inspection/AuditConsole.tsx`：AI 员工入口——澄清卡（逐题 + 跳过）、查询过程轨迹（可展开技术细节）、汇报卡（一段话结论 + 数字 + 两张图 + 发现 + 建议 + 明细折叠）、审计依据侧栏、双栏交换。
- `src/pages/training-inspection/OverviewTab.tsx`：不再内嵌对话，只保留档案内容（指标卡、最近一次审计、审计工作记录、自定义审计），用 `showConsole={false}` 接进档案视图。
- `src/pages/TrainingInspection.tsx`：页面拆成「Agent 对话（默认）」与「审计档案」两个保持挂载的视图。
- `src/components/Layout.tsx`、`src/lib/i18n.tsx`：页面与菜单统一叫「数据审计」。
