# 更新记录

## v0.3.2 · 2026-09-14

### 自定义巡检需求：Agent 对话创建 + 专属标签页

- 「问问巡检 Agent」新增创建向导：点预设「新建一个巡检需求」，或直接说「帮我新建一个巡检」，Agent 就用选项按钮逐步反问——关注方面（重复布置 / 时间集中 / 任务量超了 / 包含特定品类，可多选）→ 巡检范围（全国或某个可见区域）→ 运行周期（每天 / 每周一 / 每月 1 日 08:00）→ 起止时间（长期 / 连续 4 周 / 自定义日期），最后确认名称即可创建；每一步「选项」和「直接打字」都行——输入框一直在，会把「重复布置和时间集中都要盯」「南区」「每周一」「从今天起连续 4 周」「确认」这类说法解析成这一步的答案，解析不出来就说明能听懂什么、留在原步骤，不往下乱走；区域账号只看得到本区域，范围自动锁定，向导里随时能取消。
- 新增纯计算模块 `src/lib/inspectionRequirements.ts`：`requirementRisks()` 按「关注方面 + 区域」筛选结论，并把人数、区域都收窄到需求范围；`runRequirement()` 生成一次运行记录（扫了多少任务、看到几项问题、影响多少人、人均分钟、逐任务结论），结论与上次一致时只推进时间与连续次数、不新增记录；`requirementDue()` / `nextRunAt()` 负责到点判定（固定 08:00 印尼时间，暂停 / 结束 / 未开始都不跑）。
- 「包含特定品类」是新的关注方面（原来的「数据不全」按反馈替换掉）：任务模型新增 `InspectionTask.categories`（演示数据里为 新品 / 护肤 / 敏感肌 / 彩妆 / 服务力 / 异议处理 / 销售话术），选了这个方面后向导会多问一步品类（选项来自数据里真实出现的品类，可多选、可打字、可「以上都要」）。需求只统计含这些品类任务上的问题：再配上「重复布置」这类规则方面就是两个条件同时成立，只选品类则等于「这些品类上的问题都要」。列表、摘要与运行记录都用同一套口径，`aspectLabelOf` 会把品类名字写进「关注：包含特定品类（新品、敏感肌）」。
- 演示数据加了品类标签，`INSPECTION_SEED_VERSION` 升到 4（老存档会自动重载演示数据，自定义巡检存在另一个键里不受影响）。
- 新增本地存储 `src/lib/requirementStore.ts`（独立键 `salesboost.training-inspection.requirements.v1`）：最多同时保留 6 条，单条记录不完整自动跳过，不随演示数据重置；`useRequirements()` 让页面订阅变化。
- 页面：自定义巡检在「巡检总览 / 区域负担」后面各占一个标签页（名称 + 未读小圆点 + × 关闭并二次确认）；tab 内容是状态摘要（发现的问题 / 受影响的 BA / 人均分钟 / 最近一次运行）→ 现在关注到的问题（业务口径，可「看任务」跳区域负担定位）→ 运行记录（批次可展开核对逐任务结论）。
- 运行方式：打开页面时补跑当天错过的批次（同一天只记一次），随时可「立即巡检」，也可「暂停 / 继续」；关闭标签页等于删除该巡检及其记录。
- 自动巡检频次可以看也能改：策略新增 `autoRunMinutes`（默认 30 分钟），总览「最近一次巡检」右上角改成显示真实频次（「每 30 分钟自动巡检一次，有变化才记新批次」，60 分钟以上显示成小时），定时器也按这个间隔跑而不是写死每分钟、改了下一轮生效。跟 Agent 对话就能调：问「自动巡检多久跑一次？」会报当前设置，说「把巡检频率改成每 2 小时一次」会当场改掉并写进策略（`formatCadence` / `parseCadenceMinutes` 支持「半小时」「15 分钟」「2 小时」「每小时」等说法），刷新后保留。
- 区域负担的任务行不再直接「调整」：改成 **查看详情**，先弹出一个只读的任务详情（任务类型 / 负责人 / 起止与频次 / 单次时长 / 下发范围与命中人数 / 品类 / 本周排期 / 内容资源 / 关联任务 / 执行情况 / 人员名单 / 数据缺口），内容全部取自任务本身；外部接入任务仍保留「打开原任务」。
- 任务详情右下角的「调整任务」换成 **向创建者发送调整建议**：点一下由巡检 Agent 按当前周期的结论生成一封站内信草稿——收件人取任务创建者（取不到就落到负责人，演示数据补了 `createdByName`），正文是问题、建议动作与影响范围（本周分钟 / 涉及人数），确认「发送站内信」后提示已进入「消息中心 › 我的消息」（原型演示，不真的发送，也不改任务数据）。新增 `src/lib/adjustmentSuggestion.ts` 与单测。
- 顶部操作更自解释：周期切换前面补「统计周期」标签并给三个控件加悬停说明（点日期回到本周）；「数据接入」改名为「数据来源」并补说明（同步过来的任务缺哪些字段、回原页面补齐，缺字段只标数据不足）；「导出记录」改名「导出工作记录」，导出后用提示写清文件名与去向（下载成 `培训巡检工作记录-<周一>.txt`）。
- 去掉重复信息：页面顶部那行「最近巡检 …（自动）· 26 项任务 · 9 个批次 · 本周期 26 项任务有问题」整条删除（这些数字在「最近一次巡检」卡片与「巡检工作记录」里都有），最近一次巡检的时间、任务数、执行人、连续次数统一收到「最近一次巡检」卡片右上角，和自动巡检频次排在一起；只有本地存档异常时才保留一条提醒条。
- 「本周要花的时间」的口径说明不再单独占一行：改成指标名后面的信息 icon（`InfoTip`），鼠标移上去或键盘聚焦才浮出来，页面排版更干净，内容与原口径说明一致（含本期未计入人数）。
- 对话面板改名：`问问巡检 Agent` → `和巡检Agent对话`。
- 演示结论更贴近业务：下线「角色不适用（C4）」这类结论，新增 **G 品类覆盖** 规则（G1）——某个品类只在部分区域有任务时点名缺的区域，例如「「彩妆」在 3 个区域没有任务 / 影响 28 人 · 雅加达南区、巴厘岛区 等 3 个区域」，并在总览的结论分组里单独作为「品类覆盖」一类；建议写成「确认这个品类是不是应该覆盖全部区域，需要就给缺的区域补一条任务，或者说明本期只在部分区域上线」，不直接判成违规。
- 巡检回答更耐读：同一条结论不再用不同任务列表反复出现（按「区域 + 说法」去重），任务名超过 3 个折成「等 N 项」，单日排太满 / 必修与考试叠加不再都说成「任务量超了」。
- 顺手修掉两处口径与渲染问题：C3 同源重复会产出同 key 的双向记录，渲染与计数前统一按 key 去重（`uniqueRisks()`）；页面批次时间与自定义巡检时间统一按印尼时间展示，不再直接显示 UTC。
- 新增 `src/lib/inspectionRequirements.test.ts`（13 例）并并入 `npm run test:inspection`：方面映射、区域人数收窄、人均与缺时长口径、结论变化检测、到点判定、可见范围、向导意图与文案，「打字回答」的自然语言解析（`parseAspectsFromText` / `parseRegionFromText` / `parseCadenceFromText` / `parsePeriodFromText` / `parseReplyCommand`）以及品类筛选（`requirementCategories` / `parseCategoriesFromText` / 品类与规则方面叠加）。
- 新增 `src/lib/adjustmentSuggestion.test.ts`（2 例）并并入 `npm run test:inspection`（当前共 57 例）：收件人取任务创建者、无创建人时回落负责人、问题去重、建议动作来自巡检结论。

## v0.3.1 · 2026-09-14

### 「任务体检」重构为「区域负担」

- 标签页改名：`任务体检` → `区域负担`（tab id 仍为 `checkup`），只回答一个问题——不同区域的任务给 BA 造成了多少时间负担。
- 新增数据模块 `src/lib/inspectionBurden.ts`：`regionBurden(state, week, today, regionIds?)` 复用引擎的排期口径（`personItems` / `regionAggregates` / `taskPeriods`），输出区域人均分钟、超载人数、单日峰值、任务数（按学习 / 练习 / 考试 / 采集拆分）、重复任务数、任务占用明细、重复内容配对与超载人员每日构成；`rankRegions()` 按超载比例 → 人均超出分钟排序。
- 判定口径：超载 = 本周人均分钟 > 区域容量（未确认区域用策略默认 90 分钟/人/周）；单日过载 = 单日峰值 > 60 分钟；缺预计时长的员工不计入人均与判定，缺时长的任务按 0 分钟计入并标「缺时长」。
- 页面结构：区域对比表（总部，列为区域 / 人均分钟 / 超载人数 / 单日峰值 / 任务数 + 「点击查看」按钮）→ 区域明细（任务占用 / 重复内容 / 超载的人）→ 每人每日分钟与当天任务构成；区域账号不进对比表，直接看本区域。
- 重复内容识别沿用任务关系：`relations.duplicateTaskIds`、`sameSourceTaskIds` 与资源共享重叠，成对展示重叠人数与合并后可省分钟。
- 处置入口迁移：区域负担任务行的「调整」调用原有 `DispositionDialog`，避免删除旧任务体检后处置无入口；`CheckupTab.tsx` 与 `TaskCoverageList.tsx` 不再被引用，文件保留以便回滚。
- 从巡检总览结论点任务会切到区域负担、自动进入所属区域并高亮该任务行。
- 巡检总览的本周期指标卡全部换成业务口径：`有问题的任务`（按任务去重，附负荷叠加 / 时间集中 / 重复布置分类）、`受影响的 BA`（人数 + 区域 + 门店）、`本周要花的时间`（小时，含人均与最忙一天）、`任务挤在一起`（同几天到期或重复要求的任务数 + 进度落后项）；顶部状态行同步改为「本周期 N 项任务有问题」，不再出现「N 条结论」与规则等级计数，且所有数字都按当前账号可见范围计算（此前主数字按角色过滤、等级分布却是全公司口径）。
- 指标卡下方补一行口径说明：`本周要花的时间 = 任务资源的预计时长 × 本周应完成次数`，按「时长字段完整」的 BA 累加，是计划排期时长而非实际耗时；缺预计时长的人和任务不计入（并标出本期因此未计入的人数），同时说明「受影响的 BA」按任务命中问题的人数统计、与排期口径不同。
- 新增 `src/lib/inspectionBurden.test.ts`（8 例）并并入 `npm run test:inspection`：容量取值、排除缺时长员工、人均与任务人均分钟口径、重复内容识别、超载人员排序、区域排序、可见区域过滤。


## v0.3.0 · 2026-09-11

### 培训巡检改造：巡检工作记录 + 首屏对话 Agent

- 页面定位从「任务布置合理度审查工作台」改为「向管理者汇报 Agent 巡检工作记录」的报告页：标签页收敛为**巡检总览 / 任务体检**两个。
- 新增巡检工作记录 `InspectionState.inspectionRuns`：按批次记录覆盖周期、覆盖任务与数量、字段待补齐任务数、风险快照、逐任务结论（最严重等级 + 命中规则号）、本次新增 / 解除 / 等级变化、触发方式与执行人。
- `runInspection()` 新增第四参数 `options`：结论无变化时只更新最近一条的 `lastSeenAt` 与 `repeatCount`，出现新增 / 解除 / 等级变化 / 覆盖任务变化或跨天时新增一条；手动巡检始终新增；读取本地快照的初始化巡检传 `record: "none"`；批次最多保留 60 条。
- 同一结论同时命中本周与下周时只记一条，避免重复汇报；风险台账新增 `ruleName` / `title` 快照，使已解除的问题在工作记录里仍可复述。
- 总览重排：首屏对话 Agent → 本次巡检摘要 → 本周期指标卡 → 可展开的巡检批次记录 → 任务覆盖清单 → 数据完整度明细；原「风险清单」与「风险构成」并入任务覆盖清单的筛选与标记，统计口径在卡片上标明「仅统计当前选中周期」。
- 新增对话 Agent `src/lib/inspectionAgent.ts`：`answerInspectionQuestion(state, actor, question)` 为唯一入口，本轮由本地规则生成，覆盖「最近巡检了哪些任务 / 现在有哪些任务需要处理 / 风险最高的任务 / 哪些任务数据不全 / 上次巡检到现在有什么变化 / 某区域情况 / 某任务为什么被标记」七类意图，回答带规则编号、周期与任务来源引用，并按登录角色过滤可见范围；识别不了的问题返回可回答范围，不编造结论。接入真实模型时只替换该函数实现。
- 移除影响分析与处置中心两个标签页：删除 `ImpactTab.tsx` 与 `DispositionTab` 组件（Git 历史保留），`DispositionDialog` 独立成文件后继续由风险卡与任务体检调用；受影响员工 / 区域只保留「影响 N 人 · 区域」文字摘要。
- 顶部「导出简报」改为「导出工作记录」：批次记录、任务覆盖清单及结论、风险明细、数据完整度与处置记录附节。
- 演示数据升级为 `INSPECTION_SEED_VERSION = 3`，预置近 7 天 4 个批次（含一条已解除的历史问题），打开页面即可看到工作记录；本地存储键不变，缺少 `inspectionRuns` 的旧快照按空记录兼容。

### 措辞精简（预览反馈）

- 页面标题回到「培训巡检」，副标题由三行长句改为一句「Agent 巡检了哪些任务、发现了什么；有疑问直接在下面问它」。
- 首屏与列表去掉重复的解释性长句：状态条改为「最近巡检 09-11 08:55（自动）· 26 项任务 · 9 个批次 · 本周期 56 条结论」；Agent 卡片、批次记录、任务清单的说明各压到一行。
- 统一短标签：任务覆盖清单 → 巡检过的任务；影响分析相关表述收敛为「N 人 · 区域」；只看命中的任务 → 只看有问题的；未命中规则 → 没问题；结论未变化 → 没变化；风险解除 / 新增风险 → 解除 / 新增。
- 任务体检区块改为「1 基本信息 / 2 人群 / 3 时间与负荷 / 4 关联任务 / 5 结论」，按钮改为「原任务 / 提交处置」「提交处置」，操作提示各压到一句。
- 对话回答同步口语化：「最近一次巡检」开头只给时间、任务数、待补字段与连续次数，逐任务要点只保留「任务（类型）· 高风险 A1」，规则全名放到引用里；一次列出超过两个区域时折叠成「雅加达南区、雅加达北区 等 4 个区域」。

### 验证

- `npm run test:inspection` 扩展为 32 项：新增批次合并 / 跨天 / 手动 / 不记账 / 覆盖变化 / 60 条裁剪 / 去重共 7 项工作记录测试，以及 11 项对话 Agent 测试（意图匹配、数量一致性、证据引用、区域权限过滤、任务提问、兜底与签名一致性）。
- 浏览器验证（1440px / 390px）：两个标签页、对话预设问题与追问、批次展开、任务覆盖清单、处置弹窗提交、手动巡检写入新批次，控制台无报错、移动端无横向溢出。

## v0.2.0 · 2026-09-10

### 培训巡检重构：任务布置合理度审查工作台

- 页面从「区域负荷 + 整改」收敛为任务合理度审查：**巡检总览 / 任务体检 / 影响分析 / 处置中心** 四个标签页。
- 统一任务数据模型：任务类型、内容资源与预计时长、分发范围与实际人员快照、时间规则与达标次数、必修/选修/补训/复训权重、三级负责人、同源/前置/互斥/重复关系、执行结果与推送回传。
- 员工侧新增聚合字段：当前周期任务数、预计分钟、剩余分钟、每日峰值、必修任务数、截止日重叠、同内容重复次数、完成率与逾期率。
- 规则引擎重写为 A 负荷 / B 时间 / C 人群 / D 内容与任务关系 / E 执行 / F 数据不足 六类共 20+ 条规则，每条风险输出「结论 / 原因 / 证据 / 建议 / 影响 / 需要确认」与规则编号、阈值、结构化指标。
- 新增发布前模拟：修改目标人群、频次、截止时间、预计时长、权重、状态与提醒策略后，即时重算受影响人数、区域人均、最高单人、单日峰值与风险增减。
- 处置中心支持 8 类动作（调整任务、调整人群、调整截止时间、降低频次、合并重复任务、暂停发布、标记合理例外、转交负责人），逐条保留操作人、原始版本、字段级变更、原因、预计影响与重新巡检结果。
- 数据不足不再被当作违规：缺少时长 / 逐人分配 / 频次 / 负责人 / 版本 / 推送 / 回传时只提示补齐，缺时长的员工不参与超容量判定。
- 权限保持分离：总部可处置全部演示任务，区域培训角色只能调整本区域任务、全国任务需转交，区域经理只读。
- 总览新增首屏结论条（最危险任务 / 影响谁 / 下一步处理建议）、五项指标卡、数据完整度明细与文本简报导出；原「管理简报」改为总览导出功能。
- 本地存储升级为 `salesboost.training-inspection.v2`；14 项规则测试覆盖规则、模拟、处置与权限。

### 界面

- 按预览反馈调整：风险与任务结论统一用任务类型（学习 / 练习 / 考试 / 采集）代替规则分类胶囊，按钮文案「模拟调整」改为「调整」，去掉总览的数据完整度指标卡与风险卡内的规则参数明细，风险卡任务行改为纯文本以减少胶囊数量。
- 术语改成人话：原「人员快照仍是旧版本」改为「改版后没有重新生成员工名单」，并直接给出「策略应命中 N 人 / 名单 M 人」的差异；界面统一使用「员工名单 / 名单版本」。
- 去掉演示数据里语义含糊的「考核与学习任务未声明顺序」风险（该任务已声明前置顺序），同时收紧规则：只有同一批人的同源任务才计入 D1，避免把其他区域任务列进证据。
- 演示数据加入 `seedVersion`：本地保存的旧演示数据在新版本打开时自动重建并提示，无需手动清理缓存。
- 影响分析员工列表按区域分组，组头显示区域名、人数与人均分钟；员工行去掉职位，只保留姓名、预计分钟、任务数与峰值。
- 员工详情「预计分钟」改名为「本周期预计分钟数」，去掉「必修任务数」卡片（演示数据不区分必修 / 选修）。
- 顶部状态条去掉角色胶囊、「本地演示数据」标记与「重置演示数据」按钮，仅在本地存储异常时显示提示。
- 移除界面与导出中的 P90 术语：总览改为人均分钟、区域表去掉 P90 列、模拟结果改显示「最高单人」、A2 规则改用「少数员工负荷明显高于区域平均 / 高负荷人群人均」表述（内部计算口径不变）。
- 新增巡检工作台组件目录 `src/pages/training-inspection/`（总览、任务体检、影响分析、处置中心、风险卡、模拟面板）。
- 修复筛选控件与弹窗宽度被基础样式覆盖的问题，补充移动端 390px 布局适配。

## v0.1.0 · 2026-09-08

### 培训巡检与整改闭环

- 新增全国巡检、区域周计划、整改跟踪、日报/周报与导出。
- 按人合并总部及区域任务，计算计划与剩余分钟、人均及 P90；支持学习附加题、每日/每周练习、考试和媒体采集。
- 检查容量、历史异常、截止集中、重复安排、必修覆盖、临期进度、复核压力与数据缺失。
- 增加责任归属、整改回应、规则复查、例外审批与到期重开、同日提醒合并及逾期升级。
- 增加任务调整、完成回传、管理策略版本、证据时间线和本地持久化。
- 新增 15 项巡检规则与权限测试；支持移动端导航和巡检布局。

### 媒体与培训素材

- 增加音视频采集任务、提交记录、分析状态与审计交互。
- 增加素材库、黄金素材与合集管理，支持数字人、剧本和金句引用素材。
- 增加 BA 打卡记录与照片详情，补充媒体数据契约与界面翻译。

### 集成与文档

- 保留已有题库资产治理、考试分析、总部 BA 活跃看板与任务筛选功能。
- README 重整为产品介绍、运行说明、功能边界和演示指南；旧版更新记录保留如下。
- 版本仍为前端原型：巡检、提醒与模型分析数据的演示不代表已接入生产后台。

## 历史迭代

### 2026-07-15 总部 BA 活动监控与考试随卷字段口径修正版

本版本补齐总部看板上“全国当前已激活 BA 数”的下钻监控体验，并明确考试发布时随卷信息字段的研发实现口径。

#### 总部 BA 活动监控

- 全国培训概览首页的“全国当前已激活 BA 数”卡片改为可点击入口，点击后打开“今日 BA 活动情况”弹窗仪表板。
- BA 活动仪表板顶部展示今日状态摘要：在线、离线、正在学习、正在参加考试、正在完成任务。
- 摘要卡片支持点击筛选，下方列表会按对应状态过滤 BA；再次点击当前状态或点击“全部 BA”可恢复全量列表。
- BA 列表展示 BA、区域、在线/离线状态、Last Activity、正在进行的活动和当前进度，去掉右侧“跟进”操作列，使弹窗更聚焦监控查看。
- 弹窗移除多余的“实时更新中”提示文案，保留更轻量的筛选和列表说明。
- 当前前端使用 mock 活动数据演示实时状态，后续可替换为后端在线状态、学习进度、考试进度和任务进度聚合接口，或接 WebSocket/SSE 做实时刷新。

#### 考试发布字段口径

- 发布考试弹窗的“随卷信息填写”模块新增研发注释，说明系统侧字段和客户自填字段的边界。
- 研发口径明确为：系统自己带职位名称和地区；门店不进系统，由客户自己填；填写后进入考试结果统计，客户后续通过 Excel 导出自行治理。
- 该注释只作为原型研发说明，不改变固定信息题、岗位及格线和考试发布逻辑。

### 2026-07-14 题库题型与考试结果分析增强版

本版本把题库、组卷、考试下发和成绩复核链路继续往“可演示、可落后端”的方向推进，重点补齐新题型、随卷信息采集、岗位差异化及格线、成绩单导出和洞察分析。

#### 题库与生成题目

- 题库题型从单选、多选、判断、段落题继续扩展，新增下拉题、排序题、复选网格和文件上传题。
- 段落题用于回答者输入一段主观答案，保留参考答案、评分要点和 AI 阅卷提示字段。
- 下拉题复用选项与单答案结构，适合在 APP 端用下拉框完成单选式选择。
- 排序题支持维护多个步骤选项和正确顺序，可用于护肤步骤、客户服务流程、销售流程等场景。
- 复选网格支持维护行、列和正确匹配关系，可用于成分与功效、产品与肤质、场景与话术等矩阵题。
- 文件上传题支持设置上传说明、允许图片/视频类型、评分要点和 AI 阅卷提示，适合收集演示视频、陈列照片、实操图片等材料。
- 题库编辑抽屉和生成题目待审核预览都支持题目素材，培训师可以为题目补充图片或视频；当前前端使用本地对象 URL mock，后续可替换为真实文件上传/OSS 地址。
- `QuestionBankItem` 统一扩展了 `gridRows`、`gridColumns`、`gridCorrectAnswers`、`uploadInstructions`、`allowedUploadTypes`、`attachments` 等字段，新题型在题库、生成预览、AI 派生和搜索中共用同一套结构。
- 生成题目页的 AI 预览支持直接切换题型，预览卡片会随题型显示对应编辑控件；审核导入题库前仍需人工确认。
- 题库搜索覆盖新题型的题干、选项、网格行列、上传说明、素材名称、素材链接、标签、产品线、品牌、类别、来源文件等内容。

#### 题库分类、标签与筛选

- 题库左侧筛选树改为多选确认式筛选，支持按品牌、产品线、类别和通用能力组合筛选。
- 品牌筛选包含 Y.O.U、Dazzle Me、BNB、Lavojoy、Hued、Glam Fix。
- 产品线筛选包含护肤品、彩妆、护发、化妆品。
- 类别筛选包含粉底、气垫、唇釉、精华、洁面。
- 通用能力用于承载服务礼仪、色彩搭配、沟通开场等不直接归属某条产品线的题目。
- 筛选树分组支持折叠，底部“清空 / 确定”按钮固定展示，避免筛选项较多时按钮被滚动遮挡。
- 常用标签管理保持一套标签库，不再拆分系统标签和自定义标签；AI 生成题目优先从现有标签库匹配，若生成新标签，则在审核入库时同步加入常用标签库。
- 标签管理弹窗以大题库场景为目标，使用密集表格、搜索、排序、分页、行内改名和删除，适配成百上千标签的治理方式。

#### 考试组卷与发布

- 发布考试时支持按参加考试的角色/岗位设置不同及格分数。
- 岗位及格分数以“左侧角色下拉 + 右侧分数下拉”的方式维护，可添加多条条件。
- 默认角色包括初级 BA、高级 BA、店长、区域经理、区域培训师、区域培训师主管，及格分可在 60 到 100 之间按 5 分步进选择。
- 每次考试下发时，系统默认把两道固定信息题追加到试卷最后：
  - 请填写你的职位名称。
  - 请填写你的门店渠道。
- 两道固定信息题不进入题库、不允许培训师删除、不计分，只用于后续成绩统计和组织分析。
- 组卷页会在已选题目列表末尾展示这两道固定信息题，并在发布弹窗与发布成功弹窗中提示“固定信息题 2 道”。
- 发布后的考试任务会携带岗位及格线、固定信息题定义和试卷总题数，任务卡片中可看到对应信息。

#### 考试任务管理与成绩复核

- 已结束考试的“完整考卷与成绩单”弹窗扩展成绩列表字段：工号、姓名、地区、职位、门店、门店渠道、最终得分、及格分、是否及格、操作。
- 成绩单搜索支持姓名、工号、地区、职位、门店和门店渠道。
- 是否及格按考生职位匹配发布时配置的岗位及格线；如果没有匹配到职位规则，则回退默认及格线。
- 成绩列表支持导出 Excel。当前前端使用浏览器原生生成 `.xls` 文件，不额外引入依赖，Excel 可直接打开。
- 导出的 Excel 包含三块内容：
  - 成绩明细：包含地区、职位、门店、门店渠道、最终得分、及格分数和是否及格。
  - 成绩分布直方图：按 0-59、60-69、70-79、80-89、90-100 统计人数和占比，并用文本条形图表示分布。
  - 不同职位通过率：按职位统计参考人数、及格人数、岗位及格分数和通过率。
- 考试结果洞察弹窗新增成绩分布直方图，使用 Recharts 展示本次考试最终得分区间人数分布。
- 考试结果洞察弹窗新增不同职位通过率卡片，按职位分别展示及格线、通过率、及格人数和参考人数。
- 已结束考试 mock 数据补充多岗位及格线，用于演示不同职位不同通过标准：初级 BA 80 分，高级 BA 85 分，店长 90 分，区域培训师 90 分。

#### 看板、积分与陪练配置

- 新增积分计算工具 `src/lib/points.ts`，统一员工当月积分口径：`当月积分 = 已完成任务数 x 10 + 考试成绩`。
- 新增当月积分和门店积分说明组件，hover 后展示积分计算公式、任务完成分、考试成绩计入口径和月底冻结归档说明。
- 总部、区域、门店和人员相关看板继续使用统一积分口径，避免不同页面对积分计算出现不一致表达。
- 新增 AI 陪练提示词说明组件，提示培训师填写角色背景、场景流程、回答提示和金句使用提示会影响最终陪练效果。
- 数字人顾客、场景剧本、区域场景剧本、金句库等配置页补充提示词填写提醒，帮助培训师理解输入内容与学员端对话稳定性的关系。

#### 三语和交互文案

- 补齐本轮新增 UI 的中文、英文、印尼语文案，包括新题型、题目素材、网格题、文件上传题、固定信息题、岗位及格分数、成绩单字段、导出 Excel、成绩分布直方图和不同职位通过率。
- 题干、业务内容、文件名、标签名等仍通过 `data-i18n-skip` 保持不翻译，避免培训师录入内容被误翻译。

### 2026-07-07 题库治理体验修正版

- 题库状态流转补齐研发注释：手动录入题走“草稿 -> 已入库 -> 已归档”，AI 派生题走“待审核 -> 已入库 -> 已归档”，明确草稿和待审核题不会进入考试组卷或课件附加题候选。
- AI 旧题派生题增加生成进度反馈，生成过程中展示读取旧题、生成变体、继承分类标签和查重写入待审核等步骤，避免用户误以为点击后无响应。
- 题目列表题干支持 hover/title 查看完整题目，列表仍保持单行扫描效率。
- 分类体系新增“通用能力”顶层分类，用于承载礼仪、沟通开场、色彩搭配等不直接归属产品线的题目，后续细分交给培训师用标签管理。
- 标签体系收敛为一套“常用标签”，取消系统标签/自定义标签的产品表达；AI 生成题目优先从现有常用标签中匹配，审核入库时才把新标签同步加入常用标签库，同名自动去重。
- 常用标签增加“标签管理”弹窗，支持搜索、排序、新增、改名、删除；删除标签只解除题目关联，不删除题目。
- 标签管理弹窗改为适配大题库的密集表格，一页展示 60 个标签，去掉说明卡和统计卡，保留搜索、排序、行内改名/删除和分页信息，便于后续接服务端分页。
- 新增每题最多 5 个标签的前端约束，批量加标签、AI 派生标签和手动编辑标签都按同一规则去重和截断。

### 2026-07-06 题库资产治理增强版

- 题库升级为共享题目资产库，统一题目数据模型，题库管理、生成题目、考试组卷和课件附加题都读取同一份题库状态。
- 题库管理页新增产品线/产品分类树、标签筛选、题型/难度/状态过滤和关键词搜索，支持按全局品类体系检索题目。
- 题目支持手动新增、二次编辑、自定义标签、批量标签、归档、重复题处理，并新增多选题和简答题配置能力。
- AI 旧题派生题支持从单题或批量选择生成变体，新题默认进入“待审核”，培训师确认题干、答案、标签和分类后再审核入库。
- 生成题目页的 AI 结果改为可编辑待审核预览，确认后再导入题库；已入库题目可直接用于考试组卷和课件附加题。
- 产品笔记补充题库后端接口与落库口径，明确题目状态、题型结构、分类字段和 AI 派生题人工审核规则。

### 2026-05-14 AI 课件上传模式迭代版

- AI 智能课件生成页的上传区 mock 文件改为两个 PPTX 课件：`Lumina_新品精华_培训版.pptx` 和 `竞品_Lbrand_精华培训.pptx`，用于演示从课件源文件生成培训内容的场景。
- 上传提示文案保留多格式支持口径，明确可拖拽 `PDF`、`Word`、`PPTX` 文件至此或点击上传。
- 上传配置区新增课件生成模式选择器，默认选中“使用AI智能生成课件”。
- 课件生成模式选择器新增“保留原始课件，仅生成讲解”选项，支持后续只生成数字人讲解脚本/配音而不改写原始课件的工作流。
- 新增课件生成模式相关英文、印尼语翻译，保持三语切换时文案一致。

### 2026-05-11 品牌、三语与内容资产工作流迭代版

- 全局品牌从 `BEAUTY AI` / `BeautyAI` 统一改为 `SalesBoost AI`，同步更新浏览器标题、metadata、README、产品说明和页面内品牌露出。
- 侧边栏品牌区新增客户标识卡片，展示“服务客户 / Hebe Beauty”，用于区分平台品牌和当前服务客户；客户名保持不参与 i18n 翻译。
- 侧边栏信息架构调整：“作业考试”统一改名为“题目与考试”，避免和周期任务混淆；英文为 `Questions & Exams`，印尼语为 `Soal & Ujian`。
- 将原“作业”概念统一改名为“附加题”，专指和课件绑定的课后附加题目；英文统一为 `Additional Questions`，印尼语统一为 `Soal Tambahan`，不再使用容易和任务混淆的 `Tugas`。
- 课件与附加题链路补齐：课件编辑页顶部新增“关联附加题管理”入口，课件管理卡片 hover 操作增加“附加题”入口，点击后进入该课件对应的附加题管理页面。
- 附加题管理页 UI 文案同步更新为“课件与附加题关联”“配置课件附加题”“学员学习完此课件后自动推送已关联的附加题”，并同步三语翻译。
- 在“关联附加题管理”菜单项增加研发备注：附加题视为和课件捆绑展示，所有 APP 和导出课件展示后面都需要附加上关联附加题。
- 修复 i18n 运行时切换语言后的缓存污染问题：当文本节点已被翻译成英文或印尼语时，会通过反向字典恢复为中文源文案，再按目标语言翻译；切回中文时侧栏不再残留 `Buat Kursus`、`Manajemen...` 等印尼语文案。
- i18n 运行时不再在 `MutationObserver` 中把译文覆盖为源文案，避免动态渲染节点在多次切换语言后无法恢复中文。
- 补齐本轮新增 UI 文案的英文、印尼语翻译，包括服务客户、附加题、金句跟读预览、重置预览、金句跟读队列、APP 下载说明等。
- 各角色数据/概览页增加“下载 APP”入口，hover 后展示 Apple App Store 和 Google Play 下载入口；该入口只放在各角色首个数据/概览面板，不再放到全局顶部导航。
- 课件编辑页补齐“关联附加题管理”按钮；课件列表 hover 操作中保留生成链接、布置任务、编辑、附加题、导出等入口，并调整按钮层级，使“布置任务”作为主要操作更突出。
- 课件编辑按钮在一期置灰，并增加研发备注：一期按钮置灰，后续单独做一个课件列表可编辑能力给亚男类角色。
- 数字人顾客、场景剧本、金句库配置页新增“待生效 / 已生效”状态标识，保存后状态切换为已生效，修改内容后回到待生效。
- 数字人顾客新增“对话预览”能力，打开后可用当前数字人设定和剧本流程进行 playground 预览，便于培训师保存前检查角色语气和交互效果。
- 场景剧本新增预览能力，打开后可按当前剧本场景模拟顾客回应，培训师可以预览步骤节点和提示是否符合预期。
- 金句库预览从对话式 playground 改为移动端“金句跟读”预览，模拟 APP 端听原音、按住跟读、查看进度和上滑下一句的学习形式，不再出现聊天记录、输入框和发送按钮。
- 金句跟读预览增加产品图背景、进度 `Progress x/y`、当前 `Practice` 编号、金句大字卡片、Listen、Hold to Read、Swipe Up，以及右侧金句跟读队列和示例评分统计。
- 金句跟读预览增加研发备注：每次跟读需要做文本比对判定正确率；如果有两次正确率低于 80%，则多读一次，最多 4 次。
- 金句库产品图区域新增“AI 一键生成配图”入口，用于基于产品基础信息快速生成移动端头图视觉。
- 用户管理“已激活”状态增加研发备注：已激活定义为已下载 APP 并登录。
- 系统设置/通知渠道相关页面增加研发备注：WhatsApp Business 等其他渠道沟通配置一期不做，二期也不做，避免研发误排期。
- 课件生成流程“审核与发布”步骤保留为展示人工复核中的产品含义，移除页面上不需要展示的研发注解气泡。
- 首页和看板指标口径继续收敛：“全国注册 BA 总数”改为“全国当前已激活 BA 数”，并用小字补充未激活人数。
- 通知入口研发备注继续保留为深蓝 hover 样式，说明一期如果做通知，只提示课件或题目生成完成，后续最多加周期任务到期提醒。

### 2026-05-09 看板达标口径与档案体验修正版

- 梳理练习任务达标率口径：已结束任务保留“整体达标率”，并在详情中补充说明“达标 = 完成全部要求次数”。
- 进行中的练习任务改为按任务周期展示达标率：每日任务展示“本日达标率”，每周任务展示“本周达标率”，判断标准为当前日/周是否完成规定次数。
- 总部、区域、区域培训师主管看板同步调整进行中任务口径，练习任务展示“本日/本周 X / Y 人已达标”，考试/学习任务仍展示“X / Y 人已完成”。
- 练习任务管理页新增当前周期达标人数、达标率和规则说明，任务卡片与详情弹窗都使用同一套周期口径，避免“整体达标率”和进行中任务混用。
- 重整全国/区域员工详情页信息架构：学习与考试模块增加当月积分和积分拆分，练习/打卡模块改为近期任务情况，与组织与档案里的个人详情表达对齐。
- 重整全国/区域门店培训详情：去掉店长和编制 BA 等冗余信息，详情卡改为门店 BA 员工列表，并保留当月积分、任务完成率、最近一次考试分数等核心指标。
- 优化全国/区域排行区布局：排行 tab 直接接在下方列表卡片上，拉开与上方看板、高风险 BA 模块的间距，修复全局和区域看板中局部重叠、错位的问题。
- 修复角色切换器同步问题：左下角角色切换器改为受控原生下拉，切换用户角色后显示值会跟随当前角色同步更新。
- 全局移除“岗位”字段展示和 mock 数据依赖，人员档案、门店档案、排行表格不再出现岗位列或岗位标签。
- 修正“平均陪练习次数”为“平均练习次数”，并补齐对应英文、印尼语翻译。
- 补齐本轮新增 UI 文案的英文和印尼语翻译，包括达标规则、当前周期达标率、积分拆分、未达标状态、门店/员工排行标题等。
- 调整 i18n 运行时对动态文本节点的记录方式，确保切换角色或页面后新增渲染的中文 UI 也能继续被翻译，业务内容仍通过 `data-i18n-skip` 保持不翻译。
- 员工详情“数据波动与学习提示”增加深蓝研发备注：员工级洞察不做定期批处理，改为查看员工时按需生成，使用 SSE/WebSocket 流式输出；后端只传该员工近 30 天聚合指标和少量证据，结果按 `employeeId + 数据版本` 做短缓存，以降低模型调用和 token 消耗。

### 2026-05-09 多语言与视觉体系迭代版

- 新增全局中文、英文、印尼语三语切换能力，入口位于顶部导航语言选择器，语言偏好会写入本地存储。
- 建立前端 i18n 运行层，支持普通文本、placeholder、title、aria-label、alt 与常见动态模板翻译；通过 `data-i18n-skip` 区分培训师上传内容和系统 UI，避免把课件标题、题目、金句、脚本等业务内容误翻译。
- 补齐人员档案、门店档案、学习任务、练习任务、考试组卷、题库、附加题配置、全国/区域看板等页面的 UI 文案翻译；任务范围、分发对象、考试关联题数、审核发布步骤、AI 自动洞察等系统配置文案也纳入翻译。
- 针对英文和印尼语较长文案优化布局：侧栏标题、分段按钮、表格列宽、题型列、任务范围、按钮组和弹窗内容支持更灵活的换行与自适应宽度。
- 重做 SalesBoost AI 后台视觉体系，收敛过多高饱和色，统一为更柔和的玫瑰品牌色、暖灰底色、深色文本与克制的边框阴影。
- 调整品牌玫瑰色，避免与低分/危险/警告红色混淆；低分、撤回、停用等风险态统一使用明确红色语义。
- 新增 `visualTones` 视觉语义工具，统一进度、分数、任务状态、AI 操作按钮的颜色规则：高完成度偏绿，低完成度偏红，中间态偏琥珀或蓝色。
- 统一“进行中”“已完成”“待开始”“考试中”等任务状态标签样式；任务达标率、任务完成率、排行榜进度条、门店/人员档案进度都按完成度语义着色。
- 统一“AI 一键生成配图”“AI 一键生成剧本”“AI 一键生成金句”“AI 组卷”等 AI 操作按钮的视觉识别，减少和普通操作按钮混淆。
- 考试组卷发布成功后新增弹窗提示，引导到考试任务管理查看已发布任务；考试任务管理支持对待开始、考试中的考试执行“撤回考试”，并增加危险操作二次确认，撤回后任务从列表移除。
- 学习任务、练习任务的“停用任务”增加二次确认，避免误停用；新建任务、新建课件/剧本/数字人/产品等入口统一为左侧加号加“新建”的按钮形态。
- 调整全国/区域看板与档案页表格信息架构，将“当月积分”前移到“任务完成率”之前，保持积分和完成率的扫描顺序一致。
- 将看板中的“新增”入口统一为“查看”，点击后跳转对应管理页面，避免在总览页产生重复新建入口。
- AI 自动洞察文案补充英文/印尼语翻译，并保留给研发的离线聚合实现说明：每周扫描近 7-14 天聚合指标，先用 SQL/规则筛选候选，再把短样例交给模型总结以降低 token 消耗。
- 优秀标杆 TOP 3 补充研发备注：同分情况下按更早交卷排序。

### 2026-05-08 数字人语音配置迭代版

- 数字人顾客配置新增“语种”字段，支持中文、英文、印尼语。
- 数字人默认语种调整为印尼语，新建数字人默认使用印尼语配置。
- 数字人配置新增“选择音色”下拉，并根据所选语种联动展示对应语种音色。
- 切换语种时自动重置为该语种的默认音色，避免语种和音色配置不匹配。

### 2026-05-08 后台评审反馈迭代版

- 课件管理支持从课件卡片直接进入课件编辑页，并保留从编辑页返回课件库的路径。
- 课件、剧本、产品金句等内容配置页补充 AI 一键生成入口，缺少基础信息时给出 toast 提醒。
- 保存产品库、保存剧本等关键操作增加保存成功 toast，减少操作反馈断点。
- 产品金句图片上传说明改为更贴近移动端培训头图的方图要求，并放宽大小限制以适配二次压缩。
- 统一项目内研发备注样式为深蓝色气泡和深蓝 hover 提示；
- 首页通知、AI 自动洞察、任务查看弹窗等研发备注和交互说明补齐；任务弹窗改为更易读的单列布局。
- 课件、剧本、金句等 mock 数据中的不必要区域标识做了清理，避免总部视角出现错误的区域标签。
- AI 自动洞察补充每周离线聚合与 token 节省实现口径：先用结构化数据筛选 Top 候选，再把短样例交给模型总结。

### 2026-05-07 后台原型迭代版

- 全局品牌统一为 `SalesBoost AI`，移除旧的 Lumina / L'ÉCLAT 展示文案。
- 区域、总部、门店相关看板补充“当月积分”，与学员端学习积分口径保持一致。
- 任务监控标签拆分为“来源 + 任务类型”，例如“区域 + 考试任务”“区域 + 练习任务”。
- 高风险 BA 模块去掉过重的动作按钮和判断标签，保留基于登录、任务、考试、练习记录的风险提示。
- 员工学习详情里的练习数据统一为“练习”口径，去掉“陪练与打卡”“金句跟读连击”等不准确文案。
- 数字人顾客配置支持上传学员端使用的大图封面，并支持自定义剧本大纲或从已有场景剧本中选择。
- AI 课件生成页去掉“生成 AI 讲解”开关，保留讲解音色选择，并增加给研发的 minimax 印尼语音色列表提示。
- 产品笔记新增“自动数据统计与学习提示实现口径”，明确后端规则和数据聚合负责结论，LLM 最多负责表达。
